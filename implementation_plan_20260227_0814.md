# 실행 계획서: 지침 파일(instructions.md) 무결성 복구 및 통합 강화 (20260227_0814)

이 계획서는 최근 작업 중 소실된 `instructions.md`의 이전 핵심 지침들을 복구하고, 새로 추가된 규칙들과 통합하여 프로젝트의 모든 불변 원칙을 완벽하게 재정립하는 것을 목표로 합니다.

## 제안된 변경 사항

### 1. 지침 파일(instructions.md) 전면 재구성
- **소실된 내용 복구**: 
  - 유저 사주 및 MBTI 분석 결과 하드코딩 절대 금지 원칙 복구.
  - 운명 시그널(매칭 정보)의 알고리즘 기반 생성 원칙 복구.
  - 다국어 무결성(UTF-8, 특수문자 깨짐 방지) 지침 유지.
- **신규 규칙 통합**:
  - 파일명 명명 규칙 (`파일명_YYYYMMDD_HHMM.md`).
  - 헤더 필수 기재 규칙 (`# Header (YYYYMMDD_HHMM)`).
  - 실시간 저장 경로 (`C:\InsuranceProject\Sajuapp`).

### 2. 아티팩트 무결성 체크리스트 최신화
- 현재 프로젝트 폴더 내에 생성된 마크다운 문서들의 헤더와 파일명을 전수 조사하여 누락된 날짜/시간 정보를 보완합니다.

---

## 검증 계획

### 자동화 테스트
- `C:\InsuranceProject\Sajuapp\instructions.md` 파일을 읽어 다음 키워드들이 모두 포함되어 있는지 확인합니다:
  - `하드코딩 절대 금지`
  - `운명 시그널`
  - `파일명_YYYYMMDD_HHMM`
  - `헤더 필수 기재`

### 수동 검증
1. 사용자가 `instructions.md`를 직접 열어 이전의 DB 관련 지침과 새로운 날짜/시간 지침이 누락 없이 통합되었는지 확인합니다.
2. 새로 생성되는 `walkthrough_20260227_0814.md`가 모든 지침을 완벽히 준수하는지 최종 검토합니다.
