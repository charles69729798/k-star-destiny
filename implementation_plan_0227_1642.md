# 실행 계획서: 대용량 대화 데이터 정리 및 에이전트 성능 복구 (0227_1642)

특정 대화("Next-Gen Algorithm Im...")가 에이전트 모드에서 로딩되지 않고 블랙화면이 되는 근본 원인이 **이전 대화(`d64370ba...`)의 데이터 과부하**임을 확인했습니다. 약 4,138개의 파일과 650MB가 넘는 데이터가 누적되어 UI 엔진이 이를 파싱하는 과정에서 멈추는 현상입니다.

## 제안된 변경 사항

### 1. Bloated 대화 데이터 클린업
- **대상**: `C:\Users\pc1\.gemini\antigravity\brain\d64370ba-7db7-4720-bdb4-440d2de7350b`
- **조치 사항**:
  - **내부 캐시 삭제**: 약 400여 개의 `.resolved.N` (번호가 붙은 캐시 파일) 제거.
  - **로그 및 텍스트 정리**: 1,300여 개의 미사용 `.txt` 로그 파일 삭제.
  - **이미지 아카이빙**: 1,800여 개의 `.png` 파일을 별도의 `_archive_media` 폴더로 이동하여 UI 로딩 시 스캔 대상에서 제외.

### 2. 에이전트 캐시 초기화
- 대화 데이터 정리 후, 에이전트가 최신 상태를 반영할 수 있도록 UI 새로고침을 권장합니다.

### 3. 지침 고도화 (instructions.md)
- "장기적인 대화 시, 이미지 및 로그 데이터가 과도하게 쌓이지 않도록 주기적인 아카이브 리포트 생성을 권장한다"는 내용을 추가합니다.

---

## 검증 계획

### 자동화 테스트
- `Get-ChildItem -Recurse | Measure-Object` 명령을 통해 해당 대화의 파일 개수가 4,000개에서 500개 미만으로 줄어들었는지 확인합니다.
- 용량이 650MB에서 유의미하게(200MB 이하) 줄어들었는지 확인합니다.

### 수동 검증
1. 사용자가 문제가 된 대화("Next-Gen Algorithm Im...")를 클릭했을 때, 지연 없이 내용이 로딩되는지 확인합니다.
2. 사이드바의 로딩 스피너가 사라지고 대화 리스트가 정상적으로 표시되는지 확인합니다.
