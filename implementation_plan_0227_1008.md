# 실행 계획서: 에이전트 로딩 최적화 및 시스템 정합성 복구 (0227_1008)

이 계획서는 에이전트 매니저의 로딩 지연 문제를 해결하기 위해 시스템 예약어 파일을 제거하고, 프로젝트 루트의 혼란을 정리하며, 향후 방대한 리포트 생성을 억제하는 최적화 지침을 수립하는 것을 목표로 합니다.

## 제안된 변경 사항

### 1. 시스템 수준 장애 요인 제거 (진행 중)
- **예약어 파일 삭제**: 윈도우에서 `grep` 및 파일 스캔을 방해하던 `C:\InsuranceProject\Sajuapp\nul` 파일을 안전하게 제거했습니다.
- **추가 점검**: 다른 하위 디렉토리에 유사한 예약어 파일(CON, PRN, AUX 등)이 있는지 전수 조사합니다.

### 2. 프로젝트 루트 디렉토리 정리 (Clutter Reduction)
- **임시 파일 정리**: 작업 중 생성된 수많은 `temp_App_*.tsx` 및 `temp_index_*.css` 파일들을 별도의 `temp_backup` 폴더로 이동하거나 삭제하여 에이전트 매니저의 스캔 부하를 줄입니다.
- **아티팩트 최적화**: 이전 대화(`d64370ba...`)와 같이 수백 개의 리포트가 쌓이지 않도록, 향후 작업은 핵심 내용 위주로 압축하여 보고(Compact Reporting)하도록 지침에 추가합니다.

### 3. 지침 파일(instructions.md) 최적화 가이드 추가
- **[instructions.md](file:///C:/InsuranceProject/Sajuapp/instructions.md)**: 
  - "파일 스캔 부하 방지를 위해 프로젝트 루트에는 핵심 작업 파일만 유지한다."
  - "작업 리포트는 필요 시에만 생성하며, 불필요한 중간 단계의 Resolved 파일 생성을 지양한다."

---

## 검증 계획

### 자동화 테스트
- `dir /s /b nul, con, aux, prn` 명령을 통해 예약어 파일이 프로젝트 폴더 내에 더 이상 존재하지 않는지 확인합니다.
- `ls temp_App_*` 명령으로 루트 디렉토리의 임시 파일들이 정리되었는지 확인합니다.

### 수동 검증
1. 사용자가 에이전트 매니저를 다시 로드하여 "Agent Loading" 상태가 해소되고 대화가 정상적으로 불러와지는지 확인합니다.
2. 프로젝트 루트 폴더의 가독성이 개선되었는지 직접 확인합니다.
