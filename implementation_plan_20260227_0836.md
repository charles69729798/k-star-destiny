# 실행 계획서: 아티팩트 무결성 및 단계(Phase) 기반 명명 규칙 강화 (20260227_0836)

이 계획서는 프로젝트 관리 중 발생하는 모든 단계별 리포트(Phase/Task)에서 날짜와 시간이 누락되는 문제를 근본적으로 해결하고, 이를 지침에 명문화하여 적용하는 것을 목표로 합니다.

## 제안된 변경 사항

### 1. 지침 파일(instructions.md) 강화
- **명명 대상 구체화**: "Phase", "Step" 등 작업의 단위를 나타내는 모든 명칭 뒤에도 반드시 `(YYYYMMDD_HHMM)`을 포함하도록 규정합니다.
- **예외 없는 적용**: 단순 상태 메시지나 시스템 생성 리포트 성격의 모든 텍스트에 대해 날짜/시간 접미사를 필수화합니다.
- **불변 원칙 재강조**: DB 하드코딩 금지, 다국어 무결성 등 이전의 핵심 지침과 함께 통합 관리합니다.

### 2. 누락된 아티팩트 보완
- 사용자가 언급한 "Walkthrough Phase 23 9" 등 날짜가 누락된 것으로 판단되는 지점을 분석하여, 이후 모든 `walkthrough` 및 `implementation_plan` 헤더에 엄격히 날짜/시간을 적용합니다.

---

## 검증 계획

### 자동화 테스트
- `C:\InsuranceProject\Sajuapp\instructions.md`를 읽어 "Phase", "Step", "헤더 필수 기재" 등의 키워드와 날짜/시간 형식이 올바르게 정의되어 있는지 확인합니다.

### 수동 검증
1. 본 계획서 및 이후 생성될 `task_20260227_0836.md`의 헤더에 `(20260227_0836)`이 포함되어 있는지 확인합니다.
2. `Walkthrough Phase 23 9`와 같이 지시된 문제 상황이 더 이상 발생하지 않도록 지침이 구체화되었는지 검토합니다.
